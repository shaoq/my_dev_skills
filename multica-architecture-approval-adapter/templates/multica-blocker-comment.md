[@<instruction-owner-name>](mention://member/<member-id>)
<!-- 准确 Member mention -->

# 架构工作为什么暂停

我们已检查现有资料，但仍无法确认 `<用普通业务语言说明缺少什么>`。如果现在继续，可能造成 `<普通用户能理解的具体后果>`。

## 你只需要回答一个问题

**`<一个完整的业务问题，例如：谁负责决定 Chat 数据可以采集什么、保存多久、谁能查看，以及如何删除？>`**

## 建议你现在这样回复（推荐）

这是团队建议：

`<recommended-canonical-action>`

<!-- 根据 current RECOMMENDATION_INTENT 只渲染以下一个完整值：
ACTION <action-id>: 由我负责
ACTION <action-id>: 负责人是 <姓名或团队>
ACTION <action-id>: 我不确定，请团队给出建议
-->

- 推荐理由：`<用普通语言区分已确认事实与推断>`
- 当前置信度：`<高|中|低|未知>`
- 适用边界：`<只会补齐哪项依赖；明确不会批准方案或开始实施>`

## 其他回复何时适用

- `<alternative-canonical-action>`：`<适用条件>`；回复后 `<主要行为或后果>`。
- `<alternative-canonical-action>`：`<适用条件>`；回复后 `<主要行为或后果>`。

## 回复后会发生什么

复制推荐的完整回复后，团队会 `<验证候选负责人|继续有界只读调查>`。回复本身不会批准方案或开始实施。只有对应任务实际开始后，Issue 才会恢复为进行中；否则仍保持阻塞并说明下一步。若你只写自然语言而漏掉处理编号，原回复不会生效，系统最多提示一次可复制的准确格式。

## 查看依据（可选）

- 已检查的资料：`<简短可读列表或“当前没有可用资料”>`
- 完整调查结果：[`<可点击标题>`](<human-accessible-url>)
- 处理编号：`<action-id>`
