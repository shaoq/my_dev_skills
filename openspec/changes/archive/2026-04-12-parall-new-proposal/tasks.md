## 1. Skill 骨架与输入处理

- [x] 1.1 创建 `parall-new-proposal/SKILL.md` 文件，包含完整的 frontmatter（name、description、argument-hint）和步骤骨架
- [x] 1.2 实现 Step 0 前置检查：参数处理（无参数时用 AskUserQuestion 询问需求描述）、输入验证

## 2. 需求分析与拆解

- [x] 2.1 实现 Step 1 需求分析：读取用户需求描述，扫描现有代码结构（Glob/Grep/Read），识别功能边界
- [x] 2.2 实现功能切片拆解：按可独立交付的功能单元拆分需求，每个子方案生成 kebab-case 名称和详细描述
- [x] 2.3 实现三问判定框架：对每个候选子方案执行 Q1(独立测试) + Q2(独立理解) + Q3(独立价值) 判定，未通过者自动合并到相关提案
- [x] 2.4 实现颗粒度检测：子方案数 > 8 时标注警告，子方案数 ≤ 2 时提示建议直接用 /opsx:propose
- [x] 2.5 实现依赖图构建：分析子方案间的依赖关系，构建有向图，执行循环依赖检测（DFS 算法）
- [x] 2.6 实现 Wave 分组：使用 Kahn 算法（BFS 拓扑排序）生成分层执行计划

## 3. 用户确认与展示

- [x] 3.1 实现拆解方案展示：输出 ASCII 依赖图可视化 + Wave 分组 + 子方案列表
- [x] 3.2 实现用户确认交互：使用 AskUserQuestion 工具展示方案，支持确认/调整/取消三种操作
- [x] 3.3 实现方案调整：接收用户反馈，重新调整拆解方案并重新展示

## 4. 批量提案创建

- [x] 4.1 实现 Step 3 批量创建：按确认后的拆解方案，为每个子方案准备精确描述文本
- [x] 4.2 实现循环调用 Skill 工具执行 `/opsx:propose`，传入子方案名称和描述，等待每次完成
- [x] 4.3 实现错误处理：单个 `/opsx:propose` 失败时记录原因，询问用户是否跳过继续

## 5. 依赖注入

- [x] 5.1 实现后注入逻辑：对有依赖的子方案，使用 Read + Edit 在 `proposal.md` 末尾追加 `## Dependencies` 段
- [x] 5.2 验证注入格式：确保 `## Dependencies` 标题行 + `- <change-name>` 列表项格式与 `parall-new-worktree-apply` 解析规则兼容
- [x] 5.3 对无依赖的子方案跳过注入步骤

## 6. 总结报告

- [x] 6.1 实现总结报告输出：创建的提案列表、依赖图可视化、预估 Wave 分组、提示执行 `/parall-new-worktree-apply`
- [x] 6.2 实现部分失败报告：标注失败项及原因，展示成功项完整总结，提供后续建议
