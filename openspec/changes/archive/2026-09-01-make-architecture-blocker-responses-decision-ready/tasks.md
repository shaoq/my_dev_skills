## 1. Baseline and failing fixtures

- [x] 1.1 Verify the archived actionable-blocker baseline, GitNexus impact boundary, and same-name `uni-architecture` change mapping
- [x] 1.2 Add failing portable recommendation fixtures for unique binding, ranked/unranked candidates, unavailable evidence, unsafe role inference, and non-approval boundary
- [x] 1.3 Add failing adapter fixtures for first-screen ordering/budget, one-time correction, canonical recovery, capacity failure, wrong actor, multiple intents, and unchanged blocked state

## 2. Portable core

- [x] 2.1 Add evidence-based recommendation projection to the actionable blocker reference and artifact template
- [x] 2.2 Update `architecture-design-workflow/SKILL.md` and mandate routing while preserving standalone, topology-neutral required fields
- [x] 2.3 Verify no Multica, Team, Agent, Issue, comment, mention, URL, status, or CLI field is required by the core blocker contract

## 3. Optional Multica adapter

- [x] 3.1 Render the recommended canonical Action within the first 800 Unicode code points after the Member mention and before audit material
- [x] 3.2 Add strict `reply_correction_candidate_v1` classification and at-most-once `reply_correction_v1` evidence
- [x] 3.3 Preserve canonical consumption, accepted/active task readback, blocked rollback, no Agent mention, and no gate/state change on correction

## 4. Verification and handoff

- [x] 4.1 Run behavior fixtures, Skill safety checks, package validation, strict OpenSpec validation, link checks, diff checks, and GitNexus detect-changes
- [x] 4.2 Produce an affected-surface/acceptance record aligned with the `uni-architecture` same-name change and confirm no Multica core modification
- [x] 4.3 Install the two updated packages over the existing identities only after all local checks pass, then provide exact UNIDRAG-12 live acceptance instructions
